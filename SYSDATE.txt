Register SYSDATE procedure in EBS

SQL> CREATE OR REPLACE PROCEDURE RAZAN_PR_SYSDT (ERRBUF OUT VARCHAR2, RETCODE OUT NUMBER) IS
  2    V_DT DATE;
  3  BEGIN
  4    SELECT SYSDATE INTO V_DT FROM DUAL;
  5  
  6    FND_FILE.PUT_LINE(FND_FILE.LOG, 'YOU CAN SEE LOG DETAILS HERE!');
  7    FND_FILE.PUT_LINE(FND_FILE.OUTPUT, 'CURRENT DATE = ' || V_DT);
  8  
  9    RETCODE := 0;  
 10    ERRBUF  := NULL;
 11  
 12  EXCEPTION
 13    WHEN OTHERS THEN
 14      RETCODE := 2;  
 15      ERRBUF  := SQLERRM;
 16      FND_FILE.PUT_LINE(FND_FILE.OUTPUT, 'CHECK YOUR PROGRAM: ' || SQLERRM);
 17  END;
 18  /

Procedure created.

--------------------
